Navigation entry for the sidebar (horizontal) or bottom tab bar (`vertical`).

```jsx
<NavItem icon={<HomeIcon/>} label="Tổng quan" active onClick={...} />
<NavItem icon={<CoffeeIcon/>} label="Latte Factor" vertical onClick={...} />
```
