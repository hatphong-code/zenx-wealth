Circular progress gauge with centered content — used for the emergency-fund "months covered" hero.

```jsx
<Ring pct={53} size={140}>
  <div className="num" style={{fontSize:27}}>3.2</div>
  <div style={{fontSize:11.5,color:'var(--zx-text-soft)'}}>/ 6 tháng</div>
</Ring>
```
